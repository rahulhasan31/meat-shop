/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "themegenix.net",
      "images.unsplash.com",
      "static.vecteezy.com",
    ],
  },
};

export default nextConfig;
